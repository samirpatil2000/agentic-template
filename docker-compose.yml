version: "3.9"

services:
  app:
    build: .
    container_name: argon-app
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@db:5432/agents}
      PORT: ${PORT:-8000}
    ports:
      - "8000:8000"
